import log from '../assets/image.png';

export default  function Headers(){
    return (
        <header id="header">
        <img src={log} alt='this logo showing the investment calculator'/>
        <h1>Investement Calculator</h1>
        
        </header>
    );
}